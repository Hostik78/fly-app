# Заметки на будущее (пока без кода)

Сюда записываются идеи, которые я попросил "просто запомнить" — без изменений в самом приложении.

## Переделать анимацию волны на заставке (2026-09-15)

Пользователю не нравится текущая анимация волны на стартовой заставке. После ближайших
критических работ с базой и основным пользовательским сценарием нужно отдельно вернуться
к заставке: сначала посмотреть текущую анимацию вместе, определить, что именно ощущается
неудачно (характер движения, скорость, форма или переход), и затем предложить более
спокойный и современный вариант. Сейчас код заставки не менять.

## Предпросмотр смартфонов (DevicePreview) — возвращён как отдельный dev-инструмент (2026-09-13)

Пользователь явно попросил отказаться от идеи отдельной десктопной версии и сделать локальный
предпросмотр по смыслу как Xcode Simulator: одна мобильная Fly внутри выбираемой модели телефона.
Поэтому в `DevicePreview.tsx` теперь есть компактная панель выбора iPhone/Android, темы и масштаба.
Она работает только при `npm run dev`, полностью вырезается из production и APK и не является частью
интерфейса для реального пользователя. Старое замечание о навязчивой панели учтено: управление
занимает одну небольшую строку над устройством, а не отдельную большую боковую панель.
Этот стенд точно проверяет размеры viewport, media queries и safe areas. Сенсорный ввод, настоящий
user-agent и плотность пикселей по-прежнему проверяются отдельными профилями устройств в Playwright:
обычный iframe не может превратить мышь компьютера в реальный touchscreen.

## Использовать Plan Mode, когда проект станет больше (2026-07-23)

Пользователь попросил запомнить: по мере роста проекта нужно будет начать пользоваться
"Plan Mode" (режим, в котором сначала пишется и согласовывается план работы, и только потом
идёт само выполнение — уже отчасти то, что делает навык brainstorming). Пока проект
небольшой, это не обязательно, но когда он разрастётся — стоит вернуться к этой идее
и включить такой режим работы на постоянной основе. Пока без конкретного решения, как именно
это будет оформлено технически — просто зафиксировано намерение.

## Дизайн-вдохновение: тёмный "аудио-консоль" интерфейс (2026-07-22)

Пользователь прислал скриншот UI (не из нашего проекта - похоже на экран записи звука) и попросил
запомнить этот дизайн как ориентир на будущее. Описание для восстановления стиля:
- Почти чёрный фон, минимум цвета (в основном градации серого + один красный акцент).
- Мелкие подписи КАПСОМ моноширинным шрифтом в углах ("B - 01", "АУДИО_ВХОД", "СТАТУС") -
  ощущение технической/инженерной консоли.
- В центре - круглый аудио-визualizer: неровные радиальные полоски-"лучи" вокруг центра
  (как звуковая волна), поверх - тонкая пунктирная направляющая окружность.
- В самом центре - тёмный круг с обводкой и маленьким красным квадратом со скруглением
  (индикатор записи/стоп).
- Тонкая горизонтальная линия-разделитель, под ней обычным (не капсом) текстом статус-сообщение
  ("Установлено защищённое соединение. Захват входного потока...").
- Внизу по углам - технические показатели моноширинным шрифтом (таймер вида 00:00:12:81,
  частота дискретизации "44,1 кГц").

Не применять это к текущей ленте/карточкам сейчас - просто ориентир для будущего редизайна.

## Внешний вид карточек анкет — тёмный вариант с фото на всю карточку ОТКЛОНЁН (2026-07-22/23)

Пробовали направление: тёмный фон приложения, реальное фото на всю карточку (Unsplash), имя/фраза/параметры
поверх затемнения снизу, коралловый акцент только на кнопке лайка (вдохновлено сильными примерами
Tinder/Pure с Dribbble). Этот вариант был применён к коду, но пользователь явно попросил вернуть всё
как было ("верни всё как было" после вопроса про жёлтые "M" в списке изменённых файлов) — вариант
отклонён, код возвращён к светлой версии с цветными градиент-заглушками и буквами Ж/М вместо фото.
Если возвращаться к теме дизайна снова - не предлагать этот же тёмный/полноэкранное-фото вариант
повторно без явного запроса, начать разговор заново с чистого листа.

## Позиционирование и дизайн — не типичный сайт знакомств (2026-07-23)

Пользователь попросил зафиксировать общее направление проекта (пока без изменений в коде):

- **Цвета и элементы дизайна** — придерживаться общепринятых стандартов (то, чем пользуется
  большинство приличных продуктов: понятная типографика, доступные контрасты, привычные паттерны
  интерфейса), но при этом должен быть свой узнаваемый почерк — не просто копия чужого шаблона.
- **Это не сайт знакомств "для секса" и не сайт знакомств в привычном смысле вообще.**
  В первую очередь это что-то более деловое/обычное — площадка для общения обычных людей,
  которые оказались рядом (в аэропорту), а не про поиск романтики. Романтика может стать
  одним из направлений позже, но не как главная идея с самого начала.
- **Целевая аудитория** — обычные люди в аэропорту, которые хотят с кем-то пообщаться,
  а не искатели романтических/сексуальных знакомств.
- **По ощущению это должно быть чем-то средним между сайтом знакомств и социальной сетью** —
  не классический дейтинг-сайт и не соцсеть в чистом виде, а что-то своё между этими двумя.

**Почему это важно**: пользователь явно не хочет, чтобы проект визуально и по духу читался
как типичный дейтинг-сайт — это должно определять тон будущих решений по дизайну, копирайту
и функциям (например, не делать акцент на "паре"/свайпах-ромашках и т.п. типичных дейтинг-паттернах).
